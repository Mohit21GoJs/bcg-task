import styled from 'styled-components';

const CardBody = styled.div`
    width: 100%;
    min-height: 50px;
    .body {
        width: 90%;
        height: 50px;
        resize: none;
    }
`;

export default CardBody;
