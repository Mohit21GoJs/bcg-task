import styled from 'styled-components';

const CardTitle = styled.div`
    font-size: 24px;
    font-weight: bold;
    width: 100%;
    .title {
        width: 90%;
        resize: none;
    }
    .delete {
        cursor: pointer;
    }
`;

export default CardTitle;
